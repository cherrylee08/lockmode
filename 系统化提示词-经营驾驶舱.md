# 中小企业老板经营驾驶舱 — 系统化生成提示词

> 使用方法：在装有WorkBuddy的电脑上，复制下方提示词发送给WorkBuddy，即可自动生成完整dashboard模板。然后替换DATA_JSON中的数据即可用于不同客户。

---

## 提示词（复制以下全部内容发送给WorkBuddy）

```
请帮我生成一个单文件HTML的「中小企业老板经营驾驶舱」dashboard页面，具体要求如下：

【技术要求】
- 单个HTML文件，内嵌CSS和JS，双击即可在浏览器打开
- 使用Chart.js（CDN: https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js）做图表
- 深色侧边栏 + 浅色内容区，现代极简风格
- 左侧导航栏8个模块可切换，右侧内容区随之变化
- 涨用红色(#dc2626)、跌用绿色(#16a34a)，遵循中国股市惯例
- 响应式设计，手机端可用

【8个模块说明】

模块1-经营概览：
- 4个KPI卡片（总营收、净利润、现金余额、应收账款）
- 营收利润趋势柱状图 + 支出分类饼图
- 核心经营指标表格（毛利率、存货周转天数、应收周转、人均产值、客户集中度）

模块2-财务运营：
- 4个KPI卡片（本月收入、支出、净利润、利润率）
- 现金流走势折线图（流入/流出/净额）+ 支出分类饼图
- 应收应付明细表格（含逾期标记）

模块3-风险预警：
- 企业健康评分环形图（0-100分，conic-gradient实现）
- 五维风险雷达图（流动性、盈利能力、运营效率、客户集中度、存货管理）
- 预警事项列表（高/中/低三级，带图标和详情）

模块4-项目管理：
- 4个KPI卡片（在执行项目数、总金额、已回款、延期数）
- 项目卡片列表，每个含：名称、状态标签、合同金额、已回款、截止日期、负责人、进度条

模块5-客户管理(CRM)：
- 4个KPI卡片（活跃客户数、留存率、平均客单价、复购率）
- 客户价值矩阵气泡图（合作年限vs营收）+ 客户来源分布饼图
- 客户排行列表（含头像、信息、营收贡献）

模块6-线上获客：
- 4个KPI卡片（获客数、获客成本、线上渠道营收、询盘转化率）
- 渠道投入产出对比双轴柱状图 + 转化漏斗（CSS实现，5层）
- 渠道明细表格（渠道、投入、获客数、单客成本、营收、ROI）

模块7-手机记账：
- 左侧：今日已记账记录列表 + 记账统计表
- 右侧：手机mockup（300x580px，圆角边框），内含记账表单（类型/金额/分类/备注/关联客户）+ 最近记录

模块8-智能洗账：
- 4个KPI卡片（待对账笔数、已自动匹配、异常标记、待处理差额）
- 对账进度条（自动匹配/异常待确认/未匹配）+ 异常类型饼图
- 异常明细表格（日期、摘要、流水金额、账面金额、差额、AI判断、操作）

【数据要求】
- 所有数据内嵌在HTML中，使用JavaScript变量
- 数据区域用明显的注释标记 /* === DATA_START === */ 和 /* === DATA_END === */，方便后续替换
- 使用木材行业数据作为示例（原木采购、板材加工、家具厂客户等）
- 金额单位统一用"万元"，KPI卡片用"¥XX.X万"格式

【设计细节】
- 侧边栏宽220px，深色背景(#1a1d2e)
- KPI卡片白底圆角12px，标签12px灰色，数值24px加粗
- 图表卡片白底圆角，标题14px加粗
- 表格13px，hover行浅灰背景
- 预警项带圆形图标，三级颜色区分
- 进度条高6px圆角
- 手机mockup深色外壳，内含完整表单

【交互】
- 侧边栏点击切换模块，active状态高亮
- 图表在模块首次显示时初始化（避免隐藏区域渲染问题）
- 顶部显示当前模块名称 + 演示数据标记

请直接输出完整HTML文件，不要省略任何模块的代码。
```

---

## 数据替换指南

生成模板后，替换以下数据区域即可适配不同客户：

### 1. 企业基本信息
```
找到：sidebar-logo 中的企业名称
替换：客户的真实企业名称
```

### 2. KPI数据
```
找到：每个模块的 kpi-card 中的 kpi-value 和 kpi-change
替换：客户真实经营数据
```

### 3. 图表数据
```
找到：JavaScript中的 months, revenueData, profitData 等数组
替换：客户真实月度数据
```

### 4. 表格数据
```
找到：各模块的 <tbody> 内容
替换：客户真实的客户名单、项目信息、应收应付等
```

### 5. 行业适配
```
木材行业 → 餐饮行业：
- 原木采购 → 食材采购
- 板材加工 → 菜品制作
- 家具厂客户 → 食堂/餐厅客户
- 项目管理 → 承包合同管理
- 存货周转 → 食材保鲜周转
```

---

## 使用流程（给客户演示时）

1. 到客户电脑装好WorkBuddy（2分钟）
2. 把上方提示词发给WorkBuddy（5分钟自动生成）
3. 问客户要Excel/口头数据，替换数据区域（10分钟）
4. 浏览器打开HTML，直接给老板演示（当场）

> 从装软件到看到效果，全程20分钟以内。
