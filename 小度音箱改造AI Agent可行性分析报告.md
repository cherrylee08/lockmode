# 小度音箱 1S 改造为 AI Agent 语音终端 — 可行性分析报告

## 结论

**可以实现。** 但小度音箱 1S 的硬件资源极度受限（256MB RAM / 128MB 存储），不适合在设备端运行自定义 Agent。推荐以 **PC 端语音管线 + 蓝牙音频输出** 为主方案，小度 1S 降级为「蓝牙音箱 + 唤醒入口」。

---

## 一、硬件分析

基于小度音箱 1S 拆机资料，核心硬件规格如下：

| 部件 | 型号 / 规格 | 改造相关性 |
|------|------------|-----------|
| 主控 SoC | 晶晨 Amlogic A113X（四核 A53 @ 1.5GHz，28nm） | 音频专用芯片，社区资源少，root 难度大 |
| 内存 | 256MB DDR3（ESMT W15T1G1664A） | 极度受限，无法运行自定义 Agent |
| 存储 | 128MB NAND Flash | 仅够原厂系统，无余量部署额外程序 |
| 无线模块 | 正基 AP6236A（WiFi b/g/n + **蓝牙 4.2**） | 蓝牙可用作 A2DP 音频输出通道 |
| 麦克风 | 6 孔环形阵列（实际 3 个有效 MEMS 麦） | 拾音能力好，但无法通过蓝牙暴露给 PC |
| 扬声器 | 1.75 寸全频，6Ω，约 5W | 可作为 TTS 音频输出设备 |
| 音频链路 | ES7149 DAC + AD52068 D 类功放 | 音质可接受 |
| 串口调试 | T1/T2/T3 测试点（GND/RX/TX）在电源小板上 | 需焊接才能访问，root 的入口 |
| USB | PCB 有 Micro USB 焊盘但**未贴装** | 无 USB 接口可用 |
| 红外 | 6+1 IR LED 阵列（万能遥控功能） | 与本项目无关 |
| 电源 | 12V DC 输入 | — |

### 关键发现

1. **256MB RAM + 128MB 存储**：这是最大的瓶颈。对比小爱音箱 Pro（1GB+ RAM），小度 1S 根本没有资源运行自定义语音 Agent 程序。社区里成功刷机部署自定义 Agent 的案例，基本都是小爱音箱 Pro 等高配型号。
2. **蓝牙 4.2 可用**：AP6236A 模块支持蓝牙，小度 1S 可以作为蓝牙音箱（A2DP Sink）接收 PC 的音频输出。
3. **麦克风不可通过蓝牙暴露**：智能音箱的麦克风阵列通常只服务于本地语音处理管线，不会通过 HFP/SCO 暴露给蓝牙主机。这意味着 PC 无法通过蓝牙使用小度的麦克风。
4. **无 USB 接口**：PCB 上有焊盘但没贴装，无法通过 USB 连接 ADB。
5. **串口需焊接**：T1/T2/T3 是 PCB 上的裸测试点，需要焊接排针才能连接 USB-TTL 模块。

---

## 二、三种改造方案对比

### 方案 A：DuerOS 自定义技能桥接（零硬件改动）

**原理**：利用小度原生的唤醒词（"小度小度"）和云端语音识别。开发一个 DuerOS 自定义技能，将识别到的文本通过 HTTP 发送到 PC 上的本地服务器，本地服务器调用 WorkBuddy / Codex API，返回的文本由 DuerOS 的 TTS 播报。

**数据流**：
```
用户语音 → 小度唤醒+ASR（百度云）→ 自定义技能 → HTTP → PC本地服务器
→ WorkBuddy/Codex API → 返回文本 → DuerOS TTS 播报（百度云）
```

| 维度 | 评价 |
|------|------|
| 硬件改动 | 无 |
| 开发难度 | 中（需注册 DuerOS 开发者，开发自定义技能） |
| 延迟 | 高（全链路经过百度云，约 2-4 秒） |
| 唤醒词 | 固定为"小度小度"，不可自定义 |
| 麦克风 | 利用小度原生麦克风阵列 |
| 隐私 | 语音数据经过百度云 |
| 风险 | DuerOS 技能平台政策可能变化；技能审核流程 |
| 适用场景 | 快速验证，不想买额外硬件 |

### 方案 B：PC 端语音管线 + 蓝牙输出（推荐）

**原理**：PC 运行完整的语音处理管线。输入用独立 USB 麦克风，输出通过蓝牙 A2DP 发送到小度 1S 播放。小度 1S 仅作为蓝牙音箱使用。

**数据流**：
```
USB麦克风 → PC: 唤醒词检测(openWakeWord) → VAD+录音 → STT(FunASR/Whisper)
→ Agent路由 → WorkBuddy API / Codex API → TTS(edge-tts) → 蓝牙A2DP → 小度1S播放
```

| 维度 | 评价 |
|------|------|
| 硬件改动 | 需购买一个 USB 麦克风（约 20-50 元） |
| 开发难度 | 中（Python 语音管线开发，有成熟开源组件） |
| 延迟 | 低（本地 STT/TTS，约 0.5-1.5 秒） |
| 唤醒词 | 完全可自定义 |
| 麦克风 | 独立 USB 麦克风（小度麦克风阵列被浪费） |
| 隐私 | 全本地处理，不过云 |
| 风险 | 蓝牙连接稳定性；需要维护 PC 端服务 |
| 适用场景 | 日常使用，追求低延迟和隐私 |

### 方案 C：Root + 自定义 Agent（硬核改造）

**原理**：通过焊接 UART 串口（T1/T2/T3），获取 root 权限，修改固件，部署自定义语音 Agent 程序。类似开源项目 XiaoAI Agent 的思路，但针对小度 1S。

**数据流**：
```
小度麦克风 → 自定义Agent(运行在音箱上): 唤醒+VAD+录音 → 上传音频 → PC: STT+Agent+TTS
→ 返回音频 → 小度扬声器播放
```

| 维度 | 评价 |
|------|------|
| 硬件改动 | 需焊接串口排针，拆机 |
| 开发难度 | 极高（UART 调试、固件提取与修改、交叉编译） |
| 延迟 | 中（音频需上传到 PC 处理） |
| 唤醒词 | 可自定义 |
| 麦克风 | 完整利用小度 6 麦阵列 |
| 隐私 | 取决于 STT 部署位置 |
| 风险 | 变砖风险高；A113X 社区资源极少；256MB RAM 可能不够运行 Agent |
| 适用场景 | 硬件极客，追求完整利用设备硬件 |

### 对比总结

| 维度 | 方案 A (DuerOS) | 方案 B (PC+蓝牙) | 方案 C (Root) |
|------|:---:|:---:|:---:|
| 硬件成本 | 零 | 低（USB麦克风） | 零（需焊接工具） |
| 开发周期 | 1-2 周 | 1-2 周 | 4-8 周+ |
| 延迟 | 高 (2-4s) | **低 (0.5-1.5s)** | 中 (1-2s) |
| 唤醒词自定义 | 不可 | **可** | 可 |
| 麦克风利用 | 原生阵列 | 独立 USB 麦 | 原生阵列 |
| 变砖风险 | 无 | **无** | 高 |
| 隐私 | 过百度云 | **全本地** | 取决于实现 |
| 推荐度 | 备选 | **首选** | 不推荐 |

---

## 三、推荐方案详细设计（方案 B）

### 3.1 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    PC 桌面端 (Python 服务)                 │
│                                                          │
│  [唤醒词检测] → [VAD+录音] → [STT] → [Agent路由]         │
│  openWakeWord   silero-vad  FunASR    ┌───────┐         │
│                                       │WorkBuddy│        │
│                                       │ Codex  │        │
│                                       └───┬───┘         │
│  [TTS] ←──────────────────────────────────┘             │
│  edge-tts                                                 │
│      ↓                                                    │
│  [蓝牙 A2DP 输出]                                        │
└──────┬───────────────────────────────────┬──────────────┘
       ↑                                   ↓
  [USB 麦克风]                       [小度音箱 1S]
  (音频输入)                        (蓝牙音箱模式)
```

### 3.2 技术栈选型

| 模块 | 推荐方案 | 备选 | 理由 |
|------|---------|------|------|
| 唤醒词检测 | openWakeWord | Porcupine (Picovoice) | 开源免费，支持自定义唤醒词模型 |
| VAD 语音活动检测 | silero-vad | WebRTC VAD | 轻量准确，Python 友好 |
| 录音 | sounddevice | pyaudio | sounddevice 更稳定 |
| STT 语音识别 | FunASR (阿里开源) | Whisper (openai) | FunASR 中文识别效果好，支持实时流式 |
| TTS 语音合成 | edge-tts | CosyVoice / ChatTTS | edge-tts 免费高质量，低延迟 |
| Agent 接口 | HTTP/CLI 调用 | MCP 协议 | WorkBuddy 有 API/CLI；Codex 通过 OpenAI API |
| 蓝牙音频输出 | Windows 原生蓝牙 | pybluez | Windows 蓝牙 A2DP 最稳定 |
| 编排框架 | Python asyncio | — | 语音管线天然异步 |

### 3.3 Agent 路由设计

PC 端服务需要一个路由层，根据用户语音内容决定调用哪个 Agent：

- **唤醒词 + "问一下" / "帮我"** → WorkBuddy（通用助手、文件操作、财务等）
- **唤醒词 + "代码" / "写个" / "改一下"** → Codex（编程相关）
- **唤醒词 + 其他** → 默认 WorkBuddy

也可以用两个不同的唤醒词区分：
- "嗨七" → WorkBuddy
- "嗨码" → Codex

### 3.4 连续对话与中断

- 进入对话后保持麦克风开启，用 VAD 检测说话结束
- 支持中途打断（barge-in）：检测到用户说话时停止 TTS 播放
- 超时 15 秒无活动自动退出对话模式

---

## 四、实施路线图

### 阶段 1：环境搭建与验证（第 1 周）

1. **购买 USB 麦克风**（全向拾音，建议带降噪的会议麦克风）
2. **小度 1S 蓝牙配对**：将小度 1S 与 PC 通过蓝牙配对，设为默认音频输出设备
3. **验证蓝牙音频**：PC 播放音乐，确认小度 1S 能正常播放
4. **安装 Python 环境**：Python 3.11+，创建虚拟环境

### 阶段 2：语音管线 MVP（第 2-3 周）

1. **唤醒词检测**：安装 openWakeWord，录制并训练自定义唤醒词模型
2. **录音 + VAD**：实现按下唤醒后开始录音，VAD 检测结束后停止
3. **STT 集成**：接入 FunASR，将录音转为文本
4. **TTS 集成**：接入 edge-tts，将文本转为音频并通过蓝牙播放
5. **端到端测试**：说话 → 识别 → 回声测试（返回识别到的文本）

### 阶段 3：Agent 对接（第 3-4 周）

1. **WorkBuddy 接口**：研究 WorkBuddy 的 API/CLI 调用方式
2. **Codex 接口**：配置 OpenAI API Key，封装 Codex 调用
3. **路由层**：实现关键词路由或多唤醒词路由
4. **上下文管理**：维护对话历史，支持多轮对话

### 阶段 4：体验优化（第 4-5 周）

1. **连续对话**：实现多轮对话模式
2. **中途打断**：检测用户说话时停止 TTS
3. **音效反馈**：唤醒提示音、处理中提示音
4. **错误处理**：网络异常、Agent 超时等情况的语音反馈
5. **开机自启**：将服务设为 Windows 开机自启动

### 阶段 5：进阶（可选）

1. **方案 A 并行验证**：同时尝试 DuerOS 自定义技能方案，作为低成本的补充
2. **MCP 协议接入**：如果 WorkBuddy 支持 MCP，直接通过 MCP 协议通信
3. **本地 LLM 兜底**：部署 Ollama + Qwen，网络不可用时本地响应
4. **硬件升级**：如果 USB 麦克风效果不好，考虑 3D 打印外壳 + 麦克风阵列模块

---

## 五、风险与注意事项

### 技术风险

1. **蓝牙延迟**：A2DP 协议本身有 100-200ms 缓冲延迟。如果体感明显，可尝试调整蓝牙音频缓冲区或改用 AUX 有线连接（需拆机焊接 3.5mm 接口到功放输入端）。
2. **STT 准确率**：远场拾音 + 背景噪声会影响识别率。建议 USB 麦克风放在桌面靠近用户的位置，不要放在小度音箱上。
3. **Agent 响应时间**：WorkBuddy 和 Codex 的响应时间不确定。需要在 UI 上（通过音效）给用户"正在思考"的反馈。
4. **蓝牙连接稳定性**：Windows 蓝牙偶尔断连。建议使用 PowerShell 脚本监控连接状态并自动重连。

### 与方案 A 的组合策略

可以先花 1-2 天尝试方案 A（DuerOS 自定义技能），如果 DuerOS 平台仍然开放且支持自定义技能：
- 用方案 A 做零成本验证，确认 Agent 接口和路由逻辑
- 然后用方案 B 做正式部署，获得更低延迟和更好隐私

### 关于方案 C 的补充说明

小度 1S 的 A113X 芯片社区破解资源极少，远不如小爱音箱的 MTK 芯片有成熟的刷机工具链（MTKClient 等）。如果一定要走方案 C，建议：
- 先用 UART 串口连接，观察启动日志，获取 root shell
- 不要刷第三方固件，而是在原厂系统上部署轻量 Agent
- Agent 程序需要交叉编译为 ARMv7（A113X 是 32 位）
- 考虑把 STT/LLM/TTS 全部放在 PC 端，音箱端只负责唤醒+录音+上传+播放

---

## 六、采购清单（方案 B）

| 物品 | 用途 | 预估价格 |
|------|------|---------|
| USB 全向麦克风 | 语音输入 | 20-50 元 |
| USB-TTL 模块（CP2102） | 备用：如果后续尝试方案 C 的串口调试 | 5-10 元 |
| 排针 2.54mm | 备用：焊接到 T1/T2/T3 测试点 | 2 元 |

**总成本：约 30-60 元**（不含小度音箱本身）

---

## 七、参考项目

| 项目 | 说明 | 相关性 |
|------|------|--------|
| [XiaoAI Agent](https://github.com/stevenjoezhang/xiaoai-agent) | 小爱音箱上运行自定义语音 Agent | 方案 C 的参考，但硬件不同 |
| [Open-XiaoAI Bridge](https://github.com/coderzc/open-xiaoai-bridge) | 小爱音箱接入外部 AI Agent 的桥接器 | 方案 A/C 的架构参考 |
| [MiGPT](https://github.com/idootop/MiGPT) | 小爱音箱接入 ChatGPT | API 拦截方案的参考 |
| [openWakeWord](https://github.com/dscripka/openWakeWord) | 开源唤醒词检测 | 方案 B 唤醒词模块 |
| [FunASR](https://github.com/modelscope/FunASR) | 阿里开源语音识别 | 方案 B STT 模块 |
| [edge-tts](https://github.com/rany2/edge-tts) | 微软 Edge TTS Python 库 | 方案 B TTS 模块 |
| [Quantum Smart Speaker](https://github.com/sjackp/quantum) | ESP32 本地 AI 智能音箱 | 硬件方案参考 |

---

*文档生成时间：2026-07-24*
*作者：阿七 (WorkBuddy)*
