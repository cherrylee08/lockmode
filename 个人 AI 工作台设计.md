# 个人 AI 工作台设计

日期：2026-07-28  
状态：设计已在对话中确认，等待书面规格审核

## 1. 产品目标

个人 AI 工作台是 Codex 外层的个人工作操作界面。它不替代 Codex 的推理和执行能力，而是统一负责唤醒、上下文感知、工作展示、任务编排、成果留存和多 Agent 接入。

第一版首先实现一个真正可用的语音闭环：用户喊出“逗比”后，系统快速唤醒，理解当前正在进行的工作，通过官方 Codex Voice 协作，并在用户确认后启动 Codex 任务。后续逐步加入 AI 短剧、自媒体运营、自动化和 WorkBuddy。

成功标准：

- 正常环境下约 1 秒显示中央粒子球，约 2 秒进入回应状态。
- 能识别当前窗口、屏幕内容、项目路径、打开文件和活跃 Codex 任务。
- 用户不需要重复解释当前工作背景。
- 默认复述任务目标、范围和动作，确认后再执行。
- 支持用户主动开启“直接操作模式”。
- 执行过程持续汇报，完成后自动生成摘要、决定、成果和下一步。

## 2. 范围和阶段

### M1：Voice Core

- Windows 本地常驻唤醒器。
- 唤醒词“逗比”完全在本机识别，唤醒前音频不上传。
- 中央粒子声纹球、实时字幕和右侧伴随面板。
- 当前屏幕、窗口、选中文本、打开文件和项目状态读取。
- 官方 Codex Voice 联动。
- 确认模式和直接操作模式。
- 任务状态、阻塞和基础成果留存。

### M2：Work OS

- 今日工作台首页。
- 统一事件时间线。
- 项目阶段、任务、决定和成果知识库。
- AI 短剧项目模板。

### M3：Content OS

- AI 自媒体短视频生产线。
- AI 安装和部署图文教程生产线。
- 抖音、视频号、小红书版本派生。
- 待审核队列和教程过期提醒。

### M4：Agent Mesh

- Skill 雷达定时任务。
- WorkBuddy 连接器。
- 跨 Agent 进度编排。
- 通知和阻塞中心。
- 自建实时语音作为可替换或备用语音引擎。

第一份实施计划只覆盖 M1。M2、M3、M4 分别编写后续规格和实施计划，避免一次实现多个独立子系统。

## 3. 系统架构

系统分为五个边界清晰的层。

### 3.1 本机感知层

职责：

- 常驻系统托盘。
- 本地识别“逗比”。
- 管理麦克风、快捷键和唤醒状态。
- 获取当前应用、窗口标题、屏幕内容、选中文本、打开文件和项目路径。
- 将上下文整理成标准输入，不直接决定任务如何执行。

依赖：

- Windows 音频输入和本地唤醒词引擎。
- Windows UI Automation、屏幕捕获和必要的辅助功能权限。

### 3.2 体验层

职责：

- 渲染中央粒子球、右侧伴随面板、实时字幕和任务状态。
- 展示麦克风、屏幕读取和 Agent 连接状态。
- 提供确认模式、直接操作模式、暂停读取和停止会话入口。
- 只展示交互状态，不直接调用具体 Agent。

### 3.3 编排核心

职责：

- 接收唤醒事件和上下文包。
- 管理会话、任务、确认和执行状态机。
- 将语音请求路由到 Voice Provider。
- 将执行任务路由到 Agent Provider。
- 统一记录事件、阻塞和成果。

### 3.4 Provider 接口层

Voice Provider 提供：

- 启动或恢复语音会话。
- 接收上下文包。
- 输出监听、转写、回应和错误状态。
- 停止或中断会话。

Agent Provider 提供：

- 读取任务。
- 汇报进度。
- 提交成果。
- 报告阻塞。

第一版使用官方 Codex Voice 和 Codex Agent Provider。未来的自建实时语音、WorkBuddy 和其他 Agent 实现同一接口。

### 3.5 个人记忆与成果层

职责：

- 保存原始事件用于追溯。
- 提炼讨论摘要、关键决定、任务、成果和下一步。
- 关联项目、来源 Agent、时间、文件和版本。
- 为 Agent 生成最小必要的上下文包。

## 4. 官方 Codex Voice 的集成边界

官方资料将 Voice in Work and Codex 描述为 ChatGPT Windows 和 macOS 桌面应用能力，并未提供将该完整语音界面嵌入第三方网页的公开接口。因此第一版采用“官方能力优先、工作台悬浮层伴随”的方式：

- 工作台负责本地唤醒、粒子界面、上下文采集和状态展示。
- 工作台通过用户配置的快捷键或可用的桌面控制方式启动官方 Codex Voice。
- 官方 Codex Voice 负责实时对话和任务协调。
- 工作台只读取可稳定获得的公开状态；无法稳定获取的内部状态不做伪造。
- Provider 边界必须保持独立，以便将来接入官方接口或替换为自建实时语音。

参考：

- https://help.openai.com/en/articles/20001274
- https://help.openai.com/en/articles/20001275-chatgpt-work-and-codex

## 5. 语音交互流程

### 5.1 待机

- 应用缩在系统托盘。
- 本机唤醒词引擎监听“逗比”。
- 唤醒前的普通音频不上传、不保存。
- 屏幕读取默认不持续运行。

### 5.2 唤醒

1. 本地唤醒词命中。
2. 粒子从屏幕四周汇聚，在中央形成点状球体。
3. 球体回应“我在”或等价短句。
4. 系统开始读取当前窗口和屏幕上下文。
5. 若用户未继续说话，球体在短暂等待后自动收起，降低误唤醒影响。

### 5.3 建立对话

1. 官方 Codex Voice 会话启动或恢复。
2. 粒子球随音量和频段产生波纹、鼓包、拉伸和局部散射。
3. 实时字幕与声纹同步出现。
4. 建立会话后，中央球体平滑缩小并滑入右侧面板。
5. 侧栏展示当前项目、操作模式、转写内容和任务状态。

### 5.4 上下文继承

系统优先使用当前上下文，不先询问项目：

- 当前应用和窗口。
- 屏幕可见内容和选中文本。
- 当前项目目录和打开文件。
- 活跃 Codex 任务。
- 最近一次与当前项目相关的决定和成果。

若多个项目同时匹配或上下文不足，系统才询问用户。

### 5.5 执行门

确认模式为默认模式：

1. 系统复述目标、影响范围和准备执行的动作。
2. 用户说“开始”或等价确认。
3. 编排核心创建任务并交给 Codex。

直接操作模式由用户主动开启：

- 已授权项目内的普通读写、命令、草稿、资料整理和自动化可直接执行。
- 删除、付款、对外发送、公开发布、权限扩大等高风险动作仍需确认。
- 权限按项目和动作保存。
- 每次直接执行都写入审计记录。

### 5.6 执行与结束

- 用户可关闭语音，Codex 任务继续运行。
- 侧栏展示排队、执行、等待输入、阻塞、失败和完成状态。
- 再次喊“逗比”时，系统优先返回正在运行的任务。
- 完成后展示一句话结果、成果入口和建议下一步。
- 对话和执行结果进入统一时间线。

## 6. 视觉设计

核心视觉是由大量发光粒子组成的动态声纹球，不使用实心发光球。

状态：

- 待机：托盘常驻，屏幕无持续覆盖。
- 唤醒：粒子从四周汇聚到中央。
- 聆听：球体缓慢呼吸，边缘出现细微波动。
- 用户说话：粒子按音量和频段跳动、变形和局部散射。
- AI 回应：使用不同节奏和色彩方向，明确区分说话方。
- 已连接：球体缩进右侧伴随面板。
- 执行中：面板补充任务阶段、进度和 Agent 状态。
- 完成：粒子短暂收束，显示成果入口后收起为光点。

颜色以青蓝粒子为主，紫色用于推理和 Agent 状态，绿色用于连接和完成状态，黄色用于等待确认，红色仅用于错误或危险操作。

## 7. 工作台首页

首页回答四个问题：

1. 现在最值得推进什么？
2. 每个项目走到哪一步？
3. 哪些事情在等待用户决定？
4. Agent 和自动化刚刚完成了什么？

首页包含：

- 当前焦点项目及明确下一步。
- 项目阶段进度。
- 今日行动。
- 等待决定。
- 正在运行和被阻塞的任务。
- Codex、自动化及未来 WorkBuddy 的统一时间线。
- 下一次自动化运行时间。

首页不按 Agent 分栏。所有来源按项目和时间统一编排。

## 8. AI 短剧项目模型

预留完整阶段：

1. 核心概念。
2. 世界观架构。
3. 角色设定。
4. 核心冲突。
5. 故事大纲。
6. 分集规划。
7. 分集剧本。
8. 分镜和视觉设定。
9. 素材生成。
10. 剪辑、配音和音乐。
11. 审核与发布。
12. 数据复盘和迭代。

当前阶段为世界观架构。每个阶段包含完成条件、当前成果、待决定事项、下一步和关联讨论。

## 9. AI 自媒体运营模型

### 9.1 短视频主线

选题 → 脚本 → 拍摄或生成 → 剪辑 → 审核 → 平台版本 → 发布 → 数据复盘。

### 9.2 图文教程支线

安装方案 → 实测验证 → 截图与步骤 → 风险提示 → 排版 → 审核 → 发布 → 版本更新。

### 9.3 母内容机制

每个选题只维护一份事实、素材和核心观点。系统派生：

- 抖音版本。
- 视频号版本。
- 小红书短视频或图文版本。

平台版本可以调整标题、封面文案、时长、简介、标签和排版，但必须回链母内容。母内容中的工具版本或安装步骤变化后，系统标记可能过期的已发布教程并生成更新任务。

### 9.4 审核和发布

第一版自动生成平台版本并进入待审核区。用户确认后才发布。公开发布属于高风险动作，即使直接操作模式开启也不能跳过确认。

## 10. 自动化与多 Agent

自动化任务具有：

- 名称、触发规则和下次运行时间。
- 所属项目。
- 输入和输出。
- 最近状态。
- 审核要求。
- 失败重试和通知策略。

Skill 雷达是首个自动化模板。

WorkBuddy 和其他 Agent 通过 Agent Provider 接入。每条状态更新必须包含统一任务标识、项目、来源、时间、状态、摘要和成果引用。不同 Agent 不需要直接读取彼此的完整私有历史。

## 11. 知识库数据模型

核心对象：

- Project：工作域和长期目标。
- Stage：项目阶段和完成条件。
- Task：可执行工作及状态。
- Session：一次语音或文字协作。
- Event：状态变化、决定、阻塞和运行记录。
- Decision：带原因和影响范围的关键决定。
- Artifact：文件、文档、链接、媒体和发布版本。
- Automation：定时或事件触发流程。
- Agent：能力、连接状态和权限。
- ContextPack：提供给某次任务的最小必要上下文。

原始记录与提炼成果分开保存。搜索默认返回决定、成果和下一步，并允许下钻到原始事件。

## 12. 隐私和安全

- 唤醒词在本机识别。
- 唤醒前音频不上传、不保存。
- 屏幕读取只在唤醒后或用户明确授权的持续会话中启用。
- UI 持续显示麦克风和屏幕读取状态。
- 用户可以一键暂停麦克风、屏幕读取、直接操作模式或整个会话。
- Agent 只获得当前任务需要的上下文。
- 密钥不写入普通项目记录。
- 所有任务保留来源、权限模式、执行动作和结果。
- 高风险动作必须确认，不能被全局直接模式覆盖。

## 13. 异常处理

- 误唤醒：短暂等待，无后续语音则自动收起。
- Codex 未启动：显示启动入口，不丢失用户刚说的内容。
- Codex 未登录或 Voice 不可用：说明原因并提供恢复步骤。
- 断网：保留本地转写草稿，允许切换文字输入，恢复后继续。
- 屏幕权限不足：降级为窗口标题、项目路径和用户口述上下文。
- 上下文冲突：展示匹配项目并询问一次。
- Agent 卡住：保留现场，标记阻塞，明确需要用户决定的事项。
- 任务失败：保留错误摘要、已完成步骤和安全重试入口。
- 知识库写入失败：任务结果先进入本地待同步队列。

## 14. 测试与验收

### 单元测试

- 唤醒、聆听、对话、确认、执行、阻塞、完成和收起状态转换。
- 确认模式和直接操作模式规则。
- 高风险操作永远需要确认。
- 统一任务、事件和成果模型。
- 上下文包的最小披露规则。

### 集成测试

- 本地唤醒到界面显示。
- 当前窗口和屏幕上下文采集。
- 官方 Codex Voice 启动和恢复。
- Codex 任务创建、进度和结果采集。
- 网络、登录和权限异常恢复。

### 端到端测试

使用真实场景验证：

1. 用户正在查看一个项目。
2. 喊“逗比”。
3. 系统读取并说明当前上下文。
4. 用户讨论方案并确认。
5. Codex 执行任务。
6. 用户在侧栏看到进度和阻塞。
7. 完成后在项目时间线找到摘要、决定、成果和下一步。

## 15. 非目标

M1 不包含：

- WorkBuddy 实际接入。
- 全量工作台首页。
- AI 短剧完整工作流。
- 自媒体生产和平台发布。
- 云端持续监听唤醒词。
- 绕过官方产品权限或未公开接口。
- 无确认的公开发布、付款或不可恢复删除。

这些内容保留在总体架构中，但分阶段实现。

