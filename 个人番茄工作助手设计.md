# 个人番茄工作助手设计

日期：2026-07-30  
状态：已完成产品设计确认，等待最终文档审阅

## 1. 产品定义

“小步”是一款 Windows 优先的个人桌面番茄工作助手，面向容易拖延、启动困难和无意识分心的个人用户。

它不是监督软件，也不通过惩罚、连续打卡压力或行为监控强迫用户工作。它帮助用户完成一个更短的行为闭环：

1. 明确此刻唯一要做的任务。
2. 选择 25 分钟专注，或在难以开始时选择 3 分钟启动。
3. 用安静的桌面悬浮窗保持任务可见。
4. 在结束时记录事实，完成则休息，未完成则缩小下一步。
5. 通过低压力统计看见投入和重新回到任务的能力。

产品的核心承诺是：把“我不知道怎么开始”变成“我只需要先完成眼前这一小步”。

## 2. 首版成功标准

首版成功不以功能数量衡量，而以用户能否稳定完成一个可信闭环衡量：

- 用户可以在两分钟内创建今日任务并开始第一轮专注。
- 用户启动困难时，可以在一次点击后获得明确的 3 分钟动作。
- 应用休眠、锁屏或意外退出后，不会凭空增加或丢失一轮番茄。
- DeepSeek 不可用时，任务、计时、复盘和统计仍能使用。
- 用户可以从每日统计中理解今天投入了多少时间，而不会看到羞辱性或惩罚性评价。
- Windows 安装、托盘、系统通知和置顶悬浮窗能在真实桌面环境中稳定工作。

## 3. 首版范围

### 3.1 必须完成

- 今日任务的新增、编辑、排序、完成和删除。
- 任务优先级与预计完成时间。
- 默认 25 分钟工作、5 分钟休息，并允许用户修改默认时长。
- “我现在很难开始”3 分钟启动模式。
- 开始前选择当前任务，并明确提示“接下来只完成这一件事”。
- 专注悬浮窗、始终置顶开关、最小化到托盘和系统通知。
- 暂停、继续、提前结束和完成后的事实确认。
- 用户主动点击“我分心了”，记录一次觉察并获得温和返回提示。
- 未完成原因选择和 DeepSeek 任务拆分建议。
- DeepSeek 生成 3 分钟启动动作与每日温和总结。
- 每日番茄数、专注时长、完成任务数、主动觉察分心次数和 3 分钟启动次数。
- 本地数据导出：完整记录导出为 JSON，每日聚合统计导出为 CSV。

### 3.2 后续验证后再做

- 任务多次延期或连续未完成时的智能启动建议。
- 更丰富的周、月趋势洞察。
- macOS 安装包。
- 可选云同步与多设备使用。
- 除 DeepSeek 以外的 AI 服务商预设。

### 3.3 明确不做

- 浏览器历史、键盘输入或网页内容采集。
- 监控当前窗口、浏览器、视频或聊天软件。
- 阻止、限制或强制关闭其他软件。
- 惩罚式连续打卡、失败率、红色警告或负面排名。
- 团队管理、员工监督和多人协作。

## 4. 体验原则

### 4.1 温和，但不含糊

提示不评价人格，只描述当前状态并给出一个可执行动作。允许使用：

- “我们先完成这一小步。”
- “不用追求完美，先开始。”
- “25 分钟以后，再评价结果。”
- “没关系，人都会分心。现在只需要回到剩余时间。”

禁止使用：

- “你又偷懒了。”
- “你失败了。”
- “你浪费了时间。”

### 4.2 单一行动优先

每个关键页面只突出一个主动作。开始前突出“开始专注”，运行中突出剩余时间，结束后突出“完成了”或“还没有完成”。

### 4.3 统计用于理解，不用于评分

“主动觉察分心”被视为一次重新选择，而不是扣分项。没有足够历史数据时不展示比较结论。每日建议只给一个可尝试动作，不生成长篇评价。

### 4.4 AI 是陪伴层，不是系统依赖

AI 提供建议，不直接修改任务，不替用户确认完成结果。任何 AI 结果都必须可编辑、可拒绝；AI 不可用时，应用使用本地模板继续工作。

## 5. 核心用户流程

### 5.1 今日任务

应用启动后进入“今天”页面，标题为“今天想完成什么？”。

任务包含标题、优先级和预计用时。用户可以添加多个任务、调整顺序、完成或删除。页面默认选中最高优先级且未完成的第一个任务，但开始前仍需用户确认。

### 5.2 正常番茄

1. 用户选择任务。
2. 应用显示任务名称、25 分钟时长和提示“接下来只完成这一件事情”。
3. 用户点击“开始专注”。
4. 主窗口可自动缩成悬浮窗。
5. 悬浮窗显示任务、倒计时、暂停、结束和“我分心了”。
6. 到时后发送系统通知并进入结束确认。

### 5.3 3 分钟启动困难模式

“我现在很难开始”按钮始终显示在正常番茄入口旁。

点击后，应用把当前任务名称发送给 DeepSeek，请其生成一个三分钟内可以完成、可观察是否完成的动作。结果只包含：

- 一个动作。
- 一个完成标准。

用户可以编辑、重新生成或使用本地模板。确认动作后开始 3 分钟计时。结束时询问是否愿意继续 25 分钟，但不自动启动。

本地降级模板根据任务类型生成通用动作，例如：

- 打开相关文件。
- 写下标题。
- 列出三个要点。
- 标出第一个不知道如何处理的问题。

### 5.4 主动觉察分心

应用不读取其他软件。用户点击“我分心了”后：

1. 为当前会话记录一次自报分心事件。
2. 显示一句温和提示。
3. 提供“回到任务”和“暂停一下”两个选择。

同一轮内重复点击时，文案可以变化，但不升级为责备或强制弹窗。

### 5.5 专注结束

到时后询问：“这一轮专注完成啦。你完成了计划中的这一步吗？”

完成分支：

1. 记录一次完成番茄。
2. 让用户选择是否同时完成任务；不默认把任务标记完成。
3. 进入 5 分钟休息。
4. 休息结束后提醒用户回来，由用户决定下一轮。

未完成分支：

1. 选择“任务太大”“遇到困难”“被打断”“状态不好”或填写补充说明。
2. DeepSeek 根据当前任务、原因和补充说明给出 2 至 5 个可编辑步骤。
3. 用户确认后，步骤作为原任务的子任务保存。
4. 应用建议第一个步骤作为下一轮任务，但不自动启动。

### 5.6 休息

休息页面只推荐低刺激活动：喝水、站立、看远处和简单拉伸。避免展示视频、新闻或社交入口。

休息倒计时结束后发送通知：“休息结束，我们回来继续下一轮吧。”

### 5.7 每日总结

统计页面展示当天聚合结果：

- 完成番茄数。
- 实际专注时间。
- 完成任务数。
- 主动觉察分心次数。
- 3 分钟启动次数。

当至少有一天历史数据时，可以展示与最近一天的简单差异。DeepSeek 根据当天聚合数据生成一句观察和一个明天建议。该请求不包含完整任务标题或历史明细，除非用户主动选择包含。

## 6. 界面设计

### 6.1 视觉方向

界面采用安静、温暖的桌面感，而不是企业管理后台：

- 主色为深森林绿。
- 背景使用暖纸色。
- 关键开始按钮使用少量陶土橙。
- 大面积留白和清晰排版降低信息噪声。
- 动效只用于状态切换、计时结束和确认反馈，不使用持续跳动或刺激性动画。

### 6.2 主窗口

左侧导航包含“今天”“专注记录”“成长趋势”“设置”。“今天”页面由三部分组成：

- 当前建议任务和开始按钮。
- 今日轻量统计。
- 今日任务列表。

“我现在很难开始 · 3 分钟”与正常开始按钮并列，但视觉级别略低，避免把困难模式变成默认路径。

### 6.3 悬浮窗

悬浮窗显示：

- 当前任务。
- 剩余时间。
- 一句短提示。
- 暂停、结束和“我分心了”。

它默认可置顶但不抢输入焦点，可以隐藏到托盘。关闭悬浮窗不会结束会话。

### 6.4 结束弹窗

结束弹窗不使用红色错误状态。完成和未完成是两个并列的事实选项。未完成原因选择之后，AI 建议在独立区域展示，并明确标记“建议，可编辑”。

## 7. 技术架构

### 7.1 技术选择

- Electron 作为桌面运行时。
- React + TypeScript 作为渲染层。
- Electron 主进程负责窗口、托盘、通知、计时恢复、数据库和 AI 网络请求。
- 预加载脚本通过受限、类型化接口连接渲染层和主进程。
- 渲染层不启用 Node.js 集成，不直接访问文件系统、数据库或 API 密钥。
- 本地结构化数据使用 SQLite。
- DeepSeek 使用可配置的兼容聊天接口，首版默认模型预设为 `deepseek-chat`。
- Windows 安装包使用 `electron-builder` 生成。

### 7.2 进程边界

主进程包含以下独立模块：

- `WindowManager`：主窗口、悬浮窗、托盘和单实例行为。
- `SessionClock`：会话状态、目标结束时间、暂停累计时间和休眠恢复。
- `TaskRepository`：任务读写与事务。
- `SessionRepository`：专注会话和分心事件。
- `StatisticsService`：从原始记录计算日统计。
- `AIService`：DeepSeek 请求、超时、重试、结果验证和本地降级。
- `CredentialService`：使用操作系统能力保护 API 密钥。
- `ExportService`：导出用户可读的数据文件。

渲染层按体验拆分为：

- 今日任务页。
- 开始确认页或对话框。
- 专注悬浮窗。
- 完成复盘页或对话框。
- 休息页。
- 统计页。
- 设置页。

模块之间通过明确的数据对象通信。渲染层只能调用白名单接口，所有输入在主进程再次校验。

## 8. 数据设计

### 8.1 Task

- `id`
- `title`
- `priority`
- `estimateMinutes`
- `status`
- `parentTaskId`
- `sortOrder`
- `createdAt`
- `updatedAt`
- `completedAt`

子任务通过 `parentTaskId` 关联原任务。删除有历史会话的任务时保留必要快照，避免统计失去上下文。

### 8.2 FocusSession

- `id`
- `taskId`
- `taskTitleSnapshot`
- `mode`：正常番茄或 3 分钟启动。
- `plannedSeconds`
- `startedAt`
- `targetEndAt`
- `pausedAt`
- `totalPausedSeconds`
- `endedAt`
- `status`
- `outcome`
- `incompleteReason`
- `createdAt`
- `updatedAt`

会话状态为：

`preparing -> running -> paused -> running -> review -> completed | abandoned`

一旦进入终态，不允许再次写入完成结果。

### 8.3 DistractionEvent

- `id`
- `sessionId`
- `occurredAt`
- `source`

首版 `source` 固定为 `self_report`，确保统计名称与真实数据含义一致。

### 8.4 DailySummary

- `date`
- `focusSeconds`
- `completedPomodoros`
- `completedTasks`
- `selfReportedDistractions`
- `starterSessions`
- `aiObservation`
- `aiSuggestion`
- `generatedAt`

数值字段可由原始任务和会话重新计算。AI 文本只是附加内容，不作为事实来源。

统计口径固定如下：

- “完成番茄”只统计走完计划时长并在复盘中确认结束的正常番茄；3 分钟启动不计入番茄数。
- “专注时间”统计用户选择保留的实际运行时间，暂停时间不计入；用户选择作废的会话不计入。
- “完成任务”按任务首次进入完成状态的日期统计。
- “主动觉察分心”只统计用户点击“我分心了”产生的自报事件。

### 8.5 RuntimeState

运行状态保存当前计时类型、关联会话编号、目标结束时间和暂停信息。计时类型包括专注、3 分钟启动和休息。它用于在应用重启后恢复界面；休息状态不进入专注统计。

### 8.6 Settings

设置包含默认工作与休息时长、悬浮窗置顶、通知开关、主题、DeepSeek 接口地址和模型名称。API 密钥不写入普通设置表。

## 9. 计时与状态可靠性

倒计时显示不能只依赖渲染层的定时回调。开始会话时保存 `startedAt` 和 `targetEndAt`，显示时间由当前时间与目标结束时间计算。

暂停时保存暂停时间；恢复时把暂停时长累加到 `targetEndAt`。应用恢复、电脑唤醒或窗口重新打开时，主进程重新计算状态。

如果系统在目标结束时间之后恢复，应用不擅自判定用户完成，而是进入待确认状态：

- “这轮计时已经结束，按原计划记录。”
- “这段时间离开了，作废本轮。”
- “按实际专注时间补记。”

所有关键状态转换在向界面返回成功之前完成数据库事务。会话编号作为幂等键，避免双击或重复通知造成双重统计。

## 10. DeepSeek 集成

### 10.1 使用场景

首版只允许三类 AI 请求：

1. 为当前任务生成一个 3 分钟启动动作。
2. 根据未完成原因拆分当前任务。
3. 根据当天聚合数据生成一句观察和一个建议。

### 10.2 输出约束

AI 必须返回结构化结果，主进程校验后才交给界面。

3 分钟动作：

- `action`
- `successCriterion`

任务拆分：

- `steps`，数量 2 至 5。
- 每一步包含 `title` 和建议分钟数。

每日总结：

- `observation`
- `suggestion`

所有文本设定长度上限。无效结构、空结果或违反温和表达规则时视为失败并使用本地模板。

### 10.3 隐私

默认只发送完成当前请求必需的信息：

- 3 分钟启动：当前任务名称。
- 未完成拆分：当前任务名称、原因和用户补充说明。
- 每日总结：当天聚合数字，不含完整任务列表。

请求前在设置中明确说明数据范围。API 密钥由用户填写，使用系统安全存储保护，不写日志、不包含在导出文件中，也不暴露给渲染层。

### 10.4 失败处理

- 单次请求设置明确超时。
- 只对安全的临时错误做有限重试。
- 认证失败提示用户检查密钥。
- 限流、断网、超时或结构异常时使用本地模板。
- AI 失败不阻塞会话结束、任务保存和统计写入。

## 11. 异常处理

### 11.1 休眠与锁屏

恢复时根据持久化目标时间重新计算，不把休眠期间的界面定时器误差当成专注时间。目标时间已过则进入用户确认。

### 11.2 应用意外退出

每次状态变化立即落盘。重启时如果发现非终态会话，提供“继续”“作废”或“按实际情况补记”，不静默猜测。

### 11.3 DeepSeek 不可用

显示短而具体的状态说明，自动提供本地建议。不得用持续弹窗打断计时。

### 11.4 数据损坏

数据库启用事务和约束。应用启动时运行轻量完整性检查；无法安全恢复时保留原文件、创建诊断副本并提示用户导出或恢复，禁止直接覆盖。

### 11.5 重复操作

开始、暂停、结束和确认都使用会话状态校验。到达终态后的重复请求返回当前结果，不重复增加统计。

## 12. 测试与验收

### 12.1 自动测试

- `SessionClock` 使用可控时钟测试开始、暂停、恢复、到时、跨日和休眠恢复。
- 会话状态机测试所有合法转换，并拒绝非法或重复转换。
- 任务仓储测试新增、修改、排序、父子任务和重启持久化。
- 统计服务从原始记录重算结果，验证不会重复计数。
- AI 服务测试正常结构、超时、认证失败、限流、空内容、超长内容和无效结构。
- IPC 接口测试白名单、输入长度和类型校验。
- 关键 React 交互测试开始、暂停、完成、未完成拆分和降级提示。

### 12.2 桌面集成测试

- 主窗口与悬浮窗状态同步。
- 关闭悬浮窗不结束会话。
- 托盘隐藏、恢复和退出行为正确。
- 系统通知点击后打开正确页面。
- 单实例启动不会创建重复计时器。
- 重启应用可恢复非终态会话。

### 12.3 Windows 人工验收

- 全新安装、升级和卸载。
- 常见缩放比例下的显示。
- 锁屏、睡眠和唤醒。
- 断网、错误密钥和接口限流。
- 悬浮窗置顶、拖动、隐藏和多显示器基本行为。
- 键盘操作、焦点顺序、文本缩放和基础屏幕阅读信息。

## 13. 发布边界

首个可交付版本为单用户、本地数据、Windows 优先的可安装桌面应用。用户不配置 DeepSeek 时仍能使用完整的任务、计时、休息、复盘和统计功能，只缺少个性化生成内容。

首次启动只要求用户设置一个任务。DeepSeek 配置、默认时长和其他偏好可以随后完成，避免设置流程本身成为新的启动阻力。
