---
title: Deeper Telegram 试用码机器人自动发码方案
tags:
  - Deeper-Network
  - Telegram-Bot
  - 试用码
  - 自动化
created: 2026-09-24
updated: 2026-09-24
status: 方案草案，待接口核验
---

# Deeper Telegram 试用码机器人自动发码方案

## 当前目标

把公司现有的测试激活码生成能力接入 Telegram Bot，让俄语区等推广渠道的用户自主领取一次性 App 试用码，同时保留资格判断、防滥用、核销和审计能力。

当前只完成方案设计。用户确认后台存在“新建代码”按钮，但尚未核验正式 API、认证方式和业务规则，也未完成机器人或后端代码。

## 推荐架构

```text
推广深链 /start 参数
        ↓
Telegram Bot（语言、按钮、提示）
        ↓
试用资格服务（限流、防重复、渠道归因）
        ↓
公司激活码服务（生成、核销、过期、撤销）
        ↓
数据库与审计日志
        ↓
Bot 返回试用码和打开 App 按钮
```

- Telegram 只负责交互和消息发送，不是激活码的事实来源。
- 公司后端负责资格、生成、兑换、过期、撤销和状态查询。
- App 兑换时再次向公司后端核验，不能仅凭 Bot 消息放行试用权限。

## 用户流程

1. 用户通过带渠道参数的 Telegram 链接进入机器人并点击开始。
2. 选择俄语或英语，阅读试用条件与隐私提示。
3. 点击“领取试用码”。
4. 后端检查 Telegram 用户、App 账号、设备和活动规则是否符合资格。
5. 激活码服务生成一次性随机码并设置有效期。
6. Bot 返回试用码、有效期和“打开 App”按钮。
7. App 核销成功后将状态改为已兑换；重复使用必须失败。

## 实现优先级

### 首选：调用后台 API

- 检查“新建代码”按钮触发的网络请求，确认是否已有内部 API。
- 为 Bot 创建最小权限的服务身份，只允许生成/查询试用码，不授予后台管理权限。
- 请求需支持幂等键，避免网络重试时重复生成多个码。

### 备选：浏览器/UI 自动化

- 仅在确认没有可用 API 且短期必须上线时使用。
- 页面元素、登录会话、验证码和前端改版都会导致流程中断，不适合作为长期核心。
- 凭证不得硬编码；所有生成结果仍需写入独立审计表并防止重复发码。

## 必须具备的控制

- 一个 Telegram 用户、App 账号和设备在指定活动内只能领取规定次数。
- 激活码高随机、一次性、有有效期，并支持撤销；避免可预测序列。
- 对领取、失败、核销和管理员操作做时间戳与渠道审计。
- 对频繁申请做限流和异常阻断；必要时转人工审核。
- Telegram 用户名可变，不能作为唯一身份键；使用稳定的 Telegram 用户 ID。
- Bot Token、后台凭证、Cookie、签名密钥不得进入客户端、公开仓库或 Obsidian。

## 待确认接口清单

- 现有后台 URL 与“新建代码”按钮对应的 API/请求。
- 认证方式、最小权限角色和凭证轮换机制。
- 请求字段、返回字段、错误码、幂等和并发限制。
- 试用时长、领取条件、兑换有效期、设备绑定和撤销规则。
- App 现有核销接口及已兑换/过期状态查询。
- 生产部署位置、HTTPS 域名、Telegram webhook 和日志保留期限。
- 俄语/英语文案、客服入口、隐私告知及活动渠道参数。

## 验收标准

- 合格用户只能获得一个有效码；重复点击不重复生成。
- 码仅能核销一次，过期与撤销后不可用。
- 后台接口或 Bot 暂时失败时给出明确提示，不吞码、不重复发码。
- 渠道来源、发码、核销和异常均可追踪，但日志不记录明文密钥或不必要的个人数据。
- 管理员可以暂停活动、撤销码并查看聚合数据；高风险操作保留人工审批。

---

相关：[[AI账号运营 Index]] · [[每日核心沉淀 2026-09-24]] · [[知识库总索引]]
